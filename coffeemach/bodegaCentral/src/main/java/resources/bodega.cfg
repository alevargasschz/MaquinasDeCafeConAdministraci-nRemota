# ============================================================
# bodega.cfg — Configuracion Ice para BodegaCentral
#
# En pruebas locales mantener localhost.
# Si se despliega en red, cambie la IP por la del host real.
# ============================================================

# Valor original (comentado):
# BodegaAdapter.Endpoints = tcp -h localhost -p 10001

# Endpoint donde escucha este servidor (puerto 10001)
BodegaAdapter.Endpoints = tcp -h localhost -p 10001
